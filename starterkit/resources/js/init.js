(function () {
    "use strict";
    $('document').ready(function () {
        switch ($('meta[page-id]')) {
            case 'testing':
                window.
                break;
        }
    });
})();
